apiVersion: v1
kind: Service
metadata:
  name: lab-app
spec:
  type: {{ .Values.service.type }}
  selector:
    app: lab-app
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      nodePort: {{ .Values.service.nodePort }}