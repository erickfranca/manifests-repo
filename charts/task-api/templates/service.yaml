apiVersion: v1
kind: Service
metadata:
  name: task-api
spec:
  type: {{ .Values.service.type }}
  selector:
    app: task-api
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8083