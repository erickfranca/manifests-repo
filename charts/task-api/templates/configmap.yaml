apiVersion: v1
kind: ConfigMap
metadata:
  name: {{.Chart.Name }}
  namespace: {{ .Release.Namespace }}
  labels:
    grafana_dashboard: "1"
data:
  {{ .Release.Namespace }}.json: |
{{ .Files.Get "files/dashboards/task-api.json" | nindent 4 }}
