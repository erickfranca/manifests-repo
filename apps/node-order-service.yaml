apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: node-order-service
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/erickfranca/manifests-repo.git
    targetRevision: main
    path: charts/node-order-service
  destination:
    server: https://kubernetes.default.svc
    namespace: node-order-service
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true