apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: node-payment-service
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/erickfranca/manifests-repo.git
    targetRevision: main
    path: charts/node-payment-service
  destination:
    server: https://kubernetes.default.svc
    namespace: node-payment-service
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true